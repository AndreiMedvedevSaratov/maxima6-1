export const getCounterValue = (state) => state.counter.value;

export const getConsultations = (state) => state.consultations.consultations;

export const getCurrentFilter = (state) => state.filter.filter;

export const getCurrentStage = (state) => state.stage.stage;
